// The Catch2 runner (main) comes from the Catch2WithMain library linked in
// tests/CMakeLists.txt; this translation unit exists only to give the build a
// stable entry point that never depends on a header-define that moved between
// Catch2 major versions.
#include <catch2/catch_all.hpp>
